# Current Feature

## Status

Not Started

## Goals

<!-- Goals & requirements -->

## Notes

<!-- Any extra notes -->

## History

<!-- Keep this updated. Earliest to latest -->

1. **Add theme button (Dark/Light)** — Dark/light toggle button added to desktop and mobile navbar using Font Awesome icons (moon/sun). Toggles `body.dark-theme`, persists to `localStorage`, respects `prefers-color-scheme` on first visit. CSS vars overridden on `body.dark-theme` with spec colors (`#121212` bg, `#1e1e1e` surface, `#e5e5e5` text, `#333333` border). Fixed inherited heading contrast bug by setting `color` on `body.dark-theme`. Smooth transitions via `.theme-transitioning` class.
2. **Fix `<dl>` semantic misuse** — Replaced `<dl class="hero-metrics">` and `<dl class="contact-checks">` with `<ul>`/`<li>`, since neither block used valid `<dt>`/`<dd>` pairs. Collapsed the redundant `<dd>` wrapper inside each `.metric-card`. No CSS/JS changes needed — only class selectors were targeted and the existing global list reset already applied. See @context/features/fix-dltag-spec.md.
3. **Split script.js into ES modules** — Replaced the single 590-line `script.js` (classic `<script>`) with `main.js` as the entry point (`<script type="module" src="main.js">`) importing focused modules under `js/`: `translations.js`, `icons.js`, `data.js`, `state.js` (owns mutable `currentLang`/`activeCategory` with setters, since imported `let` bindings are read-only outside their owning module), `render.js`, `theme.js`, `menu.js`, `contact.js`. Pure structural refactor, no behavior change; verified working in browser. See @context/features/split-jscode-spec.md.
4. **Add logo to navbar/footer** — Replaced the SVG placeholder icon in the navbar and footer with the brand logo image (`images/logo/logo-v4.png`, chosen after comparing v2/v3 for a lighter blue tone and bigger visible lettering; unused variants deleted). Kept "WebDev" text next to the logo in both navbar and footer (footer previously read "WebMeșter" — fixed for consistency, including the copyright line which now reads "RCD-WebDev"). Added a favicon link in `<head>` using the same logo. Changed page title to "Codreanu Daniel | Web Development". `.nav-logo-icon` grew from 2rem to 2.75rem so the lettering reads clearly; added `.nav-logo-img` with `object-fit: cover`. Also moved `<script type="module" src="main.js">` from end of `<body>` into `<head>` with `defer`. See @context/features/logo-spec.md.
5. **Add scroll reveal animation** — New `js/reveal.js` module exports `initScrollReveal()`, called from `main.js` after the initial `setLang("ro")` render. It selects all `.section` elements, adds a `.reveal` class, and uses an `IntersectionObserver` (threshold 0.15) to add `.is-visible` once each section enters the viewport, unobserving after the first reveal (one-time, not re-triggered on scroll-back). Hero is excluded since it's already in view on load. CSS in `style.css` defines `.reveal` (opacity 0, `translateY(24px)`, 0.6s transition) and `.reveal.is-visible` (opacity 1, no translate); a `prefers-reduced-motion: reduce` query disables the transform/transition entirely. See @context/features/animation-specs.md.
6. **Add hover micro-interactions to service & pricing cards** — Service cards (`.service-card`) and pricing cards (`.pricing-card`) now lift on hover (`translateY(-4px)`) and gain a deeper shadow (`--shadow-sm` → `--shadow-md`). Service cards transition a dedicated `border-top` (transparent at rest, 3px `--primary` on hover); pricing cards transition `border-color` on all sides to `--primary`. Reused the existing `--primary` token instead of adding the new accent variable proposed in the spec, since it's already theme-safe (untouched by `body.dark-theme`). Service card icons (`.service-icon`) also shift right on hover (`translateX(4px)`) as a secondary micro-interaction. All transforms are disabled under `prefers-reduced-motion: reduce`. See @context/features/cards-styling-specs.md.
7. **Update brand primary color** — Changed `--primary` from `#4361EE` to `#2563eb` after a visual test in the browser; also updated `--primary-dark` (`#3451d1` → `#1d4ed8`) and the `--primary-10`/`--primary-20`/`--primary-5` rgba tints to match the new hue, since they're used together for buttons, links, icons, and card hover accents.
8. **Drop Font Awesome, use inline SVG for theme toggle + add language flags** — Removed the Font Awesome CDN `<link>` from `index.html`, which was loaded site-wide just for the dark/light toggle's `fa-moon`/`fa-sun` icons. Added `moon` and `sun` entries to `js/icons.js` (same style as the rest of the icon set: `currentColor` stroke, `aria-hidden`); `js/theme.js` now sets `icon.innerHTML` to `ICONS.sun`/`ICONS.moon` on a `.theme-icon` span instead of swapping a Font Awesome class. Also added small flag SVGs (RO tricolor / UK Union Jack, hardcoded with real flag colors rather than `currentColor` since flags can't be theme-tinted) before the "RO"/"ENG" text in both desktop and mobile language switchers — written directly in `index.html` rather than `js/icons.js`, since that markup is static (same precedent as the existing menu/close icons). While testing, found `.lang-btn.active` used `background: var(--card)`, which is nearly identical to the dark-theme `.lang-switcher` track background, making the active language pill invisible in dark mode — fixed by giving `.lang-btn.active` a solid `--primary` background, `--primary-fg` text, and a `--primary-dark` border (theme-independent tokens, so contrast holds in both themes); switched `.lang-btn`'s base border from `none` to `1px solid transparent` so the border doesn't shift pill size when toggling active. See @context/features/fontawesome-to-svg-spec.md.
9. **Remove hero-metrics, translate and enlarge hero-checks** — Deleted the `<ul class="hero-metrics">` stat cards (60+ projects, 98% clients, 10 days, 6 years) from the hero, along with their CSS (`.hero-metrics`, `.metric-card`, `.metric-value`, `.metric-label`, and the responsive override) and the now-unused `hero.metric1`–`4` translation keys. Kept `<ul class="hero-checks">`, but made its text translatable (`<span data-i18n="hero.check1-4">` + new `ro`/`en` entries in `js/translations.js`, since it was previously Romanian-only hardcoded text) and bumped it up visually (`font-size` 0.875rem → 1rem, `.check-icon` scoped to 20×20px within `.hero-checks` only). Tuned spacing after removal: `.hero-checks` `margin-top` raised from 0.5rem to 2.5rem (more separation from the CTAs above), and `.hero` padding made symmetric top/bottom (`5rem 0` / `6rem 0` at ≥640px, dropping the extra 1rem of bottom padding that was sized for the removed metric cards). See @context/features/remove-hero-metrics-spec.md.
10. **Small fixes: lang-switcher radius, nav CTA wording** — `.lang-switcher` and `.lang-btn` changed from `var(--radius-full)` to `var(--radius-sm)`; the literal `.btn`/CTA radius (`var(--radius)`) was tried first but at the lang switcher's ~26px height it still rendered as a full pill (radius exceeds half the box height), so a smaller fixed token was used instead to get visibly rounded corners. Also reworded the English nav CTA translation key `nav.cta` from "Get a quote" to "Request a quote".
11. **Add navbar scrollspy effect** — `.nav-link` gained a `::after` underline that animates in (`scaleX(0→1)`, 0.25s) on hover, shared with a new `.active` state so the current section's link keeps its underline without hovering; `.mobile-nav-link.active` reuses the existing hover background-tint instead (more fitting for a vertical dropdown than an underline). New `js/scrollspy.js` (`initScrollSpy`, wired into `main.js`) uses a single `IntersectionObserver` with a thin `rootMargin`-defined band near the viewport center to track which of the 5 nav-linked sections (services/pricing/portfolio/process/faq) is currently in view, keeping a `visible` Set so the active state clears once you scroll out of all tracked sections instead of lingering on the last one. Reduced-motion users get the underline without the transition. See @context/features/navbar-scrollspy-specs.md.
12. **Update pricing package cards content** — Replaced the Starter/Business card copy with the new pricing-packages spec: updated taglines, price ranges (`ro` shows RON with dot thousands separators, e.g. "800-1.000 lei" / "2.500–3.500 lei"; `en` shows EUR-only, e.g. "€160-200" / "€500-700"), and full feature lists in `js/data.js` (`pricingFeatures.ro/en.starter` — 10 items, `.business` — 13 items), translated for both languages per the spec's bilingual requirement. Added a new `pricing.business.includes` translation key ("Everything in Starter, plus:") rendered as a `<p class="pricing-includes">` above the Business feature `<ul>` in `index.html`, styled in `style.css` with a tightened `margin-top` on the adjacent list. Added a representative icon per card (`.pricing-icon`, mirrors `.service-icon` styling and its `translateX(4px)` hover shift): zap for Starter (speed), trending-up for Business (growth) — both inline SVG in `index.html` since pricing-card markup is static, not data-driven. Increased `.pricing-name` emphasis (`1.25rem`→`1.5rem`, weight `700`→`800`, added `letter-spacing: -0.02em`, matching the site's heading convention) and recolored `.pricing-price` to `var(--primary)` instead of `var(--fg)` to make prices stand out — kept the existing blue brand token rather than introducing an unprecedented green accent. Also unified the Starter card's hover border width to 2px (matching Business's permanent 2px border) instead of the previous 1px sides / 3px top mismatch, adding `border-width` to the card's hover transition. See @context/features/website-packages-specs.md.
13. **Add "Custom project" pricing card** — Added a third pricing card (EN "Custom project" / RO "Proiecte personalizate") alongside Starter/Business, for quote-based custom work rather than a fixed price. New `pricingFeatures.ro/en.custom` (10 items, RO wording refined for natural phrasing) in `js/data.js`, wired into `renderPricing()` in `js/render.js` via a new `#custom-features` list. Added `pricing.custom.name/desc/price` translation keys — the price line is prose ("Pricing based on project requirements." / "Preț stabilit în funcție de cerințele proiectului.") since there's no RON/EUR range like the other two cards, so a new `.pricing-price-quote` CSS modifier shrinks it down from the big numeric `.pricing-price` style. New `.pricing-card` in `index.html` uses a "sliders" icon (signaling customization), following the same inline-SVG precedent as Starter's zap / Business's trending-up. `.pricing-grid` lost its 2-card `max-width: 56rem` cap and gained a `repeat(3, 1fr)` column layout at ≥1024px, matching the services/portfolio/testimonials 3-column pattern. See @context/features/add-card-spec.md.
