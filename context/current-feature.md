# Current Feature

## Status

Not Started

## Goals

<!-- Goals & requirements -->

## Notes

<!-- Any extra notes -->

## History

<!-- Keep this updated. Earliest to latest -->

1. **Add theme button (Dark/Light)** — Dark/light toggle button added to desktop and mobile navbar using Font Awesome icons (moon/sun). Toggles `body.dark-theme`, persists to `localStorage`, respects `prefers-color-scheme` on first visit. CSS vars overridden on `body.dark-theme` with spec colors (`#121212` bg, `#1e1e1e` surface, `#e5e5e5` text, `#333333` border). Fixed inherited heading contrast bug by setting `color` on `body.dark-theme`. Smooth transitions via `.theme-transitioning` class.
2. **Fix `<dl>` semantic misuse** — Replaced `<dl class="hero-metrics">` and `<dl class="contact-checks">` with `<ul>`/`<li>`, since neither block used valid `<dt>`/`<dd>` pairs. Collapsed the redundant `<dd>` wrapper inside each `.metric-card`. No CSS/JS changes needed — only class selectors were targeted and the existing global list reset already applied. See @context/features/fix-dltag-spec.md.
3. **Split script.js into ES modules** — Replaced the single 590-line `script.js` (classic `<script>`) with `main.js` as the entry point (`<script type="module" src="main.js">`) importing focused modules under `js/`: `translations.js`, `icons.js`, `data.js`, `state.js` (owns mutable `currentLang`/`activeCategory` with setters, since imported `let` bindings are read-only outside their owning module), `render.js`, `theme.js`, `menu.js`, `contact.js`. Pure structural refactor, no behavior change; verified working in browser. See @context/features/split-jscode-spec.md.
4. **Add logo to navbar/footer** — Replaced the SVG placeholder icon in the navbar and footer with the brand logo image (`images/logo/logo-v4.png`, chosen after comparing v2/v3 for a lighter blue tone and bigger visible lettering; unused variants deleted). Kept "WebDev" text next to the logo in both navbar and footer (footer previously read "WebMeșter" — fixed for consistency, including the copyright line which now reads "RCD-WebDev"). Added a favicon link in `<head>` using the same logo. Changed page title to "Codreanu Daniel | Web Development". `.nav-logo-icon` grew from 2rem to 2.75rem so the lettering reads clearly; added `.nav-logo-img` with `object-fit: cover`. Also moved `<script type="module" src="main.js">` from end of `<body>` into `<head>` with `defer`. See @context/features/logo-spec.md.
5. **Add scroll reveal animation** — New `js/reveal.js` module exports `initScrollReveal()`, called from `main.js` after the initial `setLang("ro")` render. It selects all `.section` elements, adds a `.reveal` class, and uses an `IntersectionObserver` (threshold 0.15) to add `.is-visible` once each section enters the viewport, unobserving after the first reveal (one-time, not re-triggered on scroll-back). Hero is excluded since it's already in view on load. CSS in `style.css` defines `.reveal` (opacity 0, `translateY(24px)`, 0.6s transition) and `.reveal.is-visible` (opacity 1, no translate); a `prefers-reduced-motion: reduce` query disables the transform/transition entirely. See @context/features/animation-specs.md.
6. **Add hover micro-interactions to service & pricing cards** — Service cards (`.service-card`) and pricing cards (`.pricing-card`) now lift on hover (`translateY(-4px)`) and gain a deeper shadow (`--shadow-sm` → `--shadow-md`). Service cards transition a dedicated `border-top` (transparent at rest, 3px `--primary` on hover); pricing cards transition `border-color` on all sides to `--primary`. Reused the existing `--primary` token instead of adding the new accent variable proposed in the spec, since it's already theme-safe (untouched by `body.dark-theme`). Service card icons (`.service-icon`) also shift right on hover (`translateX(4px)`) as a secondary micro-interaction. All transforms are disabled under `prefers-reduced-motion: reduce`. See @context/features/cards-styling-specs.md.
7. **Update brand primary color** — Changed `--primary` from `#4361EE` to `#2563eb` after a visual test in the browser; also updated `--primary-dark` (`#3451d1` → `#1d4ed8`) and the `--primary-10`/`--primary-20`/`--primary-5` rgba tints to match the new hue, since they're used together for buttons, links, icons, and card hover accents.
8. **Drop Font Awesome, use inline SVG for theme toggle + add language flags** — Removed the Font Awesome CDN `<link>` from `index.html`, which was loaded site-wide just for the dark/light toggle's `fa-moon`/`fa-sun` icons. Added `moon` and `sun` entries to `js/icons.js` (same style as the rest of the icon set: `currentColor` stroke, `aria-hidden`); `js/theme.js` now sets `icon.innerHTML` to `ICONS.sun`/`ICONS.moon` on a `.theme-icon` span instead of swapping a Font Awesome class. Also added small flag SVGs (RO tricolor / UK Union Jack, hardcoded with real flag colors rather than `currentColor` since flags can't be theme-tinted) before the "RO"/"ENG" text in both desktop and mobile language switchers — written directly in `index.html` rather than `js/icons.js`, since that markup is static (same precedent as the existing menu/close icons). While testing, found `.lang-btn.active` used `background: var(--card)`, which is nearly identical to the dark-theme `.lang-switcher` track background, making the active language pill invisible in dark mode — fixed by giving `.lang-btn.active` a solid `--primary` background, `--primary-fg` text, and a `--primary-dark` border (theme-independent tokens, so contrast holds in both themes); switched `.lang-btn`'s base border from `none` to `1px solid transparent` so the border doesn't shift pill size when toggling active. See @context/features/fontawesome-to-svg-spec.md.
9. **Remove hero-metrics, translate and enlarge hero-checks** — Deleted the `<ul class="hero-metrics">` stat cards (60+ projects, 98% clients, 10 days, 6 years) from the hero, along with their CSS (`.hero-metrics`, `.metric-card`, `.metric-value`, `.metric-label`, and the responsive override) and the now-unused `hero.metric1`–`4` translation keys. Kept `<ul class="hero-checks">`, but made its text translatable (`<span data-i18n="hero.check1-4">` + new `ro`/`en` entries in `js/translations.js`, since it was previously Romanian-only hardcoded text) and bumped it up visually (`font-size` 0.875rem → 1rem, `.check-icon` scoped to 20×20px within `.hero-checks` only). Tuned spacing after removal: `.hero-checks` `margin-top` raised from 0.5rem to 2.5rem (more separation from the CTAs above), and `.hero` padding made symmetric top/bottom (`5rem 0` / `6rem 0` at ≥640px, dropping the extra 1rem of bottom padding that was sized for the removed metric cards). See @context/features/remove-hero-metrics-spec.md.
10. **Small fixes: lang-switcher radius, nav CTA wording** — `.lang-switcher` and `.lang-btn` changed from `var(--radius-full)` to `var(--radius-sm)`; the literal `.btn`/CTA radius (`var(--radius)`) was tried first but at the lang switcher's ~26px height it still rendered as a full pill (radius exceeds half the box height), so a smaller fixed token was used instead to get visibly rounded corners. Also reworded the English nav CTA translation key `nav.cta` from "Get a quote" to "Request a quote".
11. **Add navbar scrollspy effect** — `.nav-link` gained a `::after` underline that animates in (`scaleX(0→1)`, 0.25s) on hover, shared with a new `.active` state so the current section's link keeps its underline without hovering; `.mobile-nav-link.active` reuses the existing hover background-tint instead (more fitting for a vertical dropdown than an underline). New `js/scrollspy.js` (`initScrollSpy`, wired into `main.js`) uses a single `IntersectionObserver` with a thin `rootMargin`-defined band near the viewport center to track which of the 5 nav-linked sections (services/pricing/portfolio/process/faq) is currently in view, keeping a `visible` Set so the active state clears once you scroll out of all tracked sections instead of lingering on the last one. Reduced-motion users get the underline without the transition. See @context/features/navbar-scrollspy-specs.md.
12. **Update pricing package cards content** — Replaced the Starter/Business card copy with the new pricing-packages spec: updated taglines, price ranges (`ro` shows RON with dot thousands separators, e.g. "800-1.000 lei" / "2.500–3.500 lei"; `en` shows EUR-only, e.g. "€160-200" / "€500-700"), and full feature lists in `js/data.js` (`pricingFeatures.ro/en.starter` — 10 items, `.business` — 13 items), translated for both languages per the spec's bilingual requirement. Added a new `pricing.business.includes` translation key ("Everything in Starter, plus:") rendered as a `<p class="pricing-includes">` above the Business feature `<ul>` in `index.html`, styled in `style.css` with a tightened `margin-top` on the adjacent list. Added a representative icon per card (`.pricing-icon`, mirrors `.service-icon` styling and its `translateX(4px)` hover shift): zap for Starter (speed), trending-up for Business (growth) — both inline SVG in `index.html` since pricing-card markup is static, not data-driven. Increased `.pricing-name` emphasis (`1.25rem`→`1.5rem`, weight `700`→`800`, added `letter-spacing: -0.02em`, matching the site's heading convention) and recolored `.pricing-price` to `var(--primary)` instead of `var(--fg)` to make prices stand out — kept the existing blue brand token rather than introducing an unprecedented green accent. Also unified the Starter card's hover border width to 2px (matching Business's permanent 2px border) instead of the previous 1px sides / 3px top mismatch, adding `border-width` to the card's hover transition. See @context/features/website-packages-specs.md.
13. **Add "Custom project" pricing card** — Added a third pricing card (EN "Custom project" / RO "Proiecte personalizate") alongside Starter/Business, for quote-based custom work rather than a fixed price. New `pricingFeatures.ro/en.custom` (10 items, RO wording refined for natural phrasing) in `js/data.js`, wired into `renderPricing()` in `js/render.js` via a new `#custom-features` list. Added `pricing.custom.name/desc/price` translation keys — the price line is prose ("Pricing based on project requirements." / "Preț stabilit în funcție de cerințele proiectului.") since there's no RON/EUR range like the other two cards, so a new `.pricing-price-quote` CSS modifier shrinks it down from the big numeric `.pricing-price` style. New `.pricing-card` in `index.html` uses a "sliders" icon (signaling customization), following the same inline-SVG precedent as Starter's zap / Business's trending-up. `.pricing-grid` lost its 2-card `max-width: 56rem` cap and gained a `repeat(3, 1fr)` column layout at ≥1024px, matching the services/portfolio/testimonials 3-column pattern. See @context/features/add-card-spec.md.
14. **Add hero intro animation + title accent color** — New shared keyframe `fade-slide-up` (renamed from `hero-title-fade-up`, which had only been wired to `.hero-subtitle`) fades hero elements in from `opacity: 0` / `translateY(12px)`, staggered on page load: `.hero-title` at 150ms delay, `.hero-subtitle`/`.hero-ctas`/`.hero-checks` together at 350ms, and `.badge` last at 550ms — all 600ms duration, `both` fill mode, disabled under `prefers-reduced-motion: reduce`. Also split the `hero.title` translation key into `hero.titleStart` (plain) + `hero.titleHighlight` (wrapped in new `.hero-title-accent`, colored `var(--primary)`) in `js/translations.js` for `ro`/`en`, since `setLang` re-renders `data-i18n` elements via `textContent` and would've stripped an inline `<span>` nested inside a single translated string — colors "aduc clienți afacerii tale" / "bring customers to your business" to match the pricing card price color. See @context/features/hero-intro-animation-spec.md.
15. **Refactor pricing card features, fix price wrap & hover layout shift, widen cards** — Replaced the Starter/Business/Custom feature bullet lists in `js/data.js` (`pricingFeatures.ro/en`) per the new spec: Starter trimmed 10→9 items (dropped the standalone "Google Analytics" line, reworded the SEO/support bullets), Business fully replaced its 13-item list with a new 9-item one (dropped portfolio/testimonials/FAQ/blog/competitor-analysis items, added dynamic features, online booking, admin panel, online directory listing — translated independently for `en` rather than mirrored from `ro`, since the spec's EN section, though mistakenly pasted in Romanian, used distinctly different wording from the RO "Profesional" list), Custom trimmed 10→9 items (dropped "Database-powered features"). Renamed the RO Business card to "Profesional" in `js/translations.js` (`pricing.business.name`; `en` stays "Business"). Added a new delivery-time line (`pricing.*.delivery` translation keys, rendered via `<p class="pricing-delivery">` in `index.html`, styled in `style.css` to match `.pricing-includes`) under each card's feature list, above the CTA button. While testing, found hovering the Custom (EN) card grew all 3 cards' height: `.pricing-card:hover` was animating `border-width` 1px→2px, shrinking content width by ~2px — enough to wrap the new EN delivery line to a second line, and because `.pricing-grid` uses default `align-items: stretch`, the whole row grew to match. Fixed by giving `.pricing-card` a constant `border: 2px solid` (matching the featured Business card) and transitioning only `border-color` on hover, eliminating the layout shift entirely. Also reduced `.pricing-price` from `2.5rem` to `2rem` so the RO Business price ("2.500–3.500 lei") stays on one line, and added a `--max-w-wide` (84rem) token + `.container-wide` utility (mirroring the existing `.container-narrow` pattern), applied to the pricing section only, to give the now-denser cards more breathing room than the standard 72rem container used elsewhere. See @context/features/pricing-cards-refactor.md.
16. **Update Business card delivery estimate** — Changed `pricing.business.delivery` in `js/translations.js` from "Livrabil estimat: 10–15 zile lucrătoare" / "Estimated delivery: 10–15 business days" to "15–20 zile lucrătoare" / "15-20 business days" for both `ro` and `en`.
17. **Benefits cards refactor** — Reworded the delivery benefit in `js/data.js` (`const benefits`): RO "Livrare rapidă" → "Termene predictibile", EN "Fast delivery" → "Clear timeline" (sentence case, matching the rest of the array), with new non-literal descriptions for both languages. Fixed a missing diacritic in the security benefit's RO description (`functioneze` → `funcționeze`). Reworded the Direct Communication benefit's copy on request: EN now "You work directly with the person developing your project.", RO "Discuți direct cu persoana care dezvoltă proiectul, fără intermediari." (dropped the old call-center/WhatsApp framing). Gave `.benefit-card`/`.benefit-icon` in `style.css` the same hover treatment as `.service-card`/`.service-icon` instead of inventing new classes: transparent `border-top` that fades to `--primary` on hover, `translateY(-4px)` lift with deeper shadow, icon `translateX(4px)` shift, and added both to the existing `prefers-reduced-motion: reduce` block. See @context/features/benefits-cards-refactor-specs.md.
18. **Changes out of Claude conversation** — Reworded the hero-checks bullets in `js/translations.js` for both `ro`/`en`: `hero.check2` shortened from "SEO de bază"/"Basic SEO" to "SEO", `hero.check3` changed from "Analytics" to "Design responsive"/"Responsive design", and `hero.check4` changed from "Design responsive"/"Responsive design" to "2+ Ani Experiență"/"2+ Years Experience" (dropping the Analytics bullet in favor of an experience callout). Also collapsed `pricing.business.desc` (en) from a two-line declaration to one line — formatting only, no text change. Fixed a resulting mismatch in `index.html`'s static fallback spans: lines 115/119 (`hero.check3`/`hero.check4`) still held the pre-edit text in swapped order, which would briefly show stale wording before `setLang()` overwrote it on load — corrected to match the new `translations.js` values.
19. **Remove testimonials section** — Deleted the testimonials `<section>` from `index.html` and `renderTestimonials()` (plus its call site) from `js/render.js`, since the section is no longer used. Also cleaned up everything that became dead code as a result: the `testimonials` array in `js/data.js`, the `testimonials.title`/`testimonials.subtitle` keys in `js/translations.js` (`ro`/`en`), the `.testimonials-grid`/`.testimonial-card`/`.stars`/`.testimonial-quote`/`.testimonial-author`/`.testimonial-avatar`/`.testimonial-name`/`.testimonial-role` rules and their dark-theme/responsive overrides in `style.css`, and the now-unused `star` icon in `js/icons.js`.
20. **Refactor FAQ content** — Replaced all 5 entries in `const faqs` (`js/data.js`) with 6 new bilingual FAQ items per @context/features/refactor-faq-specs.md: pricing flexibility/custom estimates, hosting & domain not included in price, adding features later (simple vs. backend-level complexity), ~2-3 week build timeline, free post-launch support, and payment via invoice. The spec listed "FAQ-4" twice with identical content, so it collapsed to 6 unique entries rather than 7. The payment FAQ's first sentence was revised on request after initial implementation, RO: "Plata se realizează pe baza unei facturi emise după livrarea proiectului și aprobarea acestuia de către client.", EN: "Payment is made based on an invoice issued after the project is delivered and approved by the client." (previously tied the invoice to "services provided" rather than delivery/approval). FAQ rendering logic in `js/render.js` was untouched — content-only change. See @context/features/refactor-faq-specs.md.
21. **Contact section refactor** — Added a `tel:+40733154058` call button next to the WhatsApp button, both wrapped in a new `.contact-actions` flex container (reused `.btn-outline` for the phone button, inline SVG matching the existing icon style). Updated `contact.subtitle` (ro/en) to drop the "în maxim 24 de ore"/"within 24 hours" promise, and reworded the second `.contact-check` bullet to "Oferta este gratuită."/"The quote is free." Removed the "Tipul afacerii" business-type form group entirely, along with everything it left dead: the `businessTypes` data object (`js/data.js`), the `contact.business` translation key, the `renderContactSelect()` function and its import/call in `js/render.js`, and the `select`-specific CSS in `.form-group`. Also added a `data-i18n-placeholder` mechanism (mirrors the existing `data-i18n` text loop in `setLang()`, but sets `.placeholder` instead) and used it for a Message textarea placeholder and an "exemplu@email.com"/"example@email.com" Email placeholder. Added client-side email validation in `js/contact.js`: since the form has `novalidate` (which had silently disabled all native browser validation, the actual cause of "no email validation"), submit now checks the email against a regex, blocking submission with an inline translated error (`contact.emailError`) and a `.input-invalid` red border (new theme-independent `--danger` token) until corrected. See @context/features/contact-refactor-spec.md.
22. **Contact form validation refactor** — Replaced the custom email-regex validation (added in #21) with native HTML5 validation per @context/features/form-validation-spec.md: removed `novalidate` from `#contact-form`, added `minlength="2" maxlength="100"` to the Name input, and `required minlength="20" maxlength="2000"` to the Message textarea (which previously had no validation at all). `js/contact.js` lost the `EMAIL_PATTERN` regex and the `showEmailError`/`clearEmailError` helpers — since the browser now blocks the `submit` event from firing at all on an invalid form, the handler just calls `preventDefault()` and shows the success UI. Removed everything that became dead as a result: the `contact.emailError` translation key (`ro`/`en`), the `#email-error` span in `index.html`, and the `.input-invalid`/`.form-error` CSS rules plus the `--danger` token (its only consumer).
23. **Add live character counter to Message textarea** — Added `<span id="message-counter" class="char-counter">0 / 2000</span>` under the Message textarea in `index.html`. New `initMessageCharacterCounter()` in `js/contact.js` (called from `initContactForm()`) reads the cap directly from `message.maxLength` rather than hardcoding it, so the counter always matches the textarea's `maxlength="2000"`, and updates the text on every `input` event. Styled with a new `.char-counter` rule in `style.css` using the existing theme-aware `--muted-fg` token (already overridden under `body.dark-theme`) and `justify-self: end` to right-align it within the `.form-group` grid, consistent with `.pricing-delivery`'s helper-text styling. No translation keys needed since the format is digits/slash only. No changes to existing `required`/`minlength`/`maxlength` validation.
24. **Wire contact form to Formspree** — Added `action="https://formspree.io/f/mlgyjlpz"` and `method="POST"` to `#contact-form` in `index.html` (the `name`, `email`, `message` fields already had `name` attributes). `js/contact.js`'s submit handler now does an async `fetch` to `form.action`/`form.method` with `FormData` and `Accept: application/json`, only showing the success UI on an `ok` response — previously it called `preventDefault()` and showed success unconditionally without ever submitting anywhere. Added a localized inline error (`contact.error` key in `js/translations.js` for `ro`/`en`, rendered via a new `<p id="contact-error" class="form-error" data-i18n="contact.error">` between the message field and the submit button) shown on a non-OK response or network failure (`catch`), keeping the form visible so the user can retry; re-added the `--danger` token (`#dc2626`) and `.form-error` rule in `style.css` (same values used previously, before they were removed in #22 since their only consumer was deleted). Reworded `contact.success` (`ro`/`en`) per request, synced with the static fallback `<p class="contact-success-msg">` text in `index.html`. The success view now auto-reverts after `SUCCESS_DISPLAY_MS` (6000ms): `form.reset()` clears the fields, the character counter is re-synced via the `updateMessageCounter` function returned by `initMessageCharacterCounter()`, and both elements' `display` are restored to their original state (`form.style.display = ""` falls back to the stylesheet's `grid`, `success.style.display = "none"` re-hides it).
