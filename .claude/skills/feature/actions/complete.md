# Complete Action

1. Reset current-feature.md:
   - Change H1 back to `# Current Feature`, no other word.
   - Clear Goals and Notes sections (keep placeholder comments)
   - Add feature summary to the END of History
2. Stage all changes, including the updated curent-feature.md and commit with a descriptive message
3. Switch to main and merge the feature branch (no push yet)
4. Delete the local feature branch
6. Push main to origin ONCE (single push with all changes)
7. If feature branch was previously pushed, delete it from origin

## Notes:

Do NOT create a separate `chore:` commit for resetting current-feature.md.
Include the reset in the feature commit before staging and committing.